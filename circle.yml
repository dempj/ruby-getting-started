deployment:
  staging:
    branch: master2
    heroku:
      appname: dempj-app-staging
  production:
    tag: /v[0-9]+(\.[0-9]+)*/
    branch: master2
    heroku:
      appname: dempj-app-production
  production:
    tag: /v[0-9]+(\.[0-9]+)*/
    branch: /hotfix2\/.*/
    heroku:
      appname: dempj-app-production
